machine:
  timezone:
    Europe/Minsk
  #python:
  #  version: 2.7.5
  services:
    - docker
  
  
dependencies:
  pre:
    - pip install awscli docker-compose==1.6.2
    
test:
  pre:
    - docker-compose build
